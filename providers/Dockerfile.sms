FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install fastapi uvicorn httpx pydantic

# Copy the providers directory
COPY providers/ /app/providers/

EXPOSE 8000

ENV PYTHONPATH=/app
CMD ["python", "providers/sms_provider.py"]
